version: 2.1

jobs:
  build:
    docker:
      - image: 'cimg/openjdk:17.0'
    steps:
      - checkout
      - run:
          name: Analyze on SonarCloud
          command: mvn verify sonar:sonar -Dsonar.projectKey=YoshinNaidoo_apds7311-poe-cyber-wolves

workflows:
  main:
    jobs:
      - build:
          context: SonarCloud
